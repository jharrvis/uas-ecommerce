// Timer is now handled via useCountdown hook in the parent page
// This component is kept as a thin wrapper for backwards compatibility
// The actual timer display is in LeftPanel and the topbar of UjianPage

export default function Timer() {
  return null
}
